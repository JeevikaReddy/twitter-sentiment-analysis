<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sentiment Analysis Result</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            text-align: center;
            padding: 50px;
        }
        .container {
            max-width: 600px;
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        img {
            border: 1px solid #ddd;
            border-radius: 5px;
            margin-top: 10px;
            max-width: 100%;
            height: auto;
        }
        .btn-custom {
            margin-top: 10px;
        }
    </style>
</head>
<body>

    <div class="container">
        <h2 class="mb-4">Sentiment Analysis Result</h2>

        <h4>Sentiment Counts:</h4>
        <ul class="list-group mb-3">
            {% for sentiment, count in sentiment_counts.items() %}
                <li class="list-group-item">{{ sentiment }}: <strong>{{ count }}</strong></li>
            {% endfor %}
        </ul>

        <h4>Sentiment Distribution:</h4>
        <img src="{{ plot_path }}" alt="Sentiment Distribution">

        <br>

        <!-- Download button -->
        <a href="/download" class="btn btn-success btn-custom">Download Results</a>

        <!-- Upload another file button -->
        <a href="/" class="btn btn-primary btn-custom">Upload Another File</a>
    </div>

</body>
</html>
